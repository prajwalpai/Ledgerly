import { NextRequest,NextResponse } from "next/server";import { discoverDriveInbox,syncDriveInbox } from "@/lib/drive-sync";import { assertLocalRequest,safeApiError } from "@/lib/security";
export const runtime="nodejs";export async function GET(request:NextRequest){try{assertLocalRequest(request);return NextResponse.json({configured:true,folderName:"Ledgerly Financial Inbox",available:Boolean(discoverDriveInbox())});}catch(error){const r=safeApiError(error);return NextResponse.json({configured:false,error:error instanceof Error?error.message:r.message},{status:404});}}export async function POST(request:NextRequest){try{assertLocalRequest(request);return NextResponse.json(await syncDriveInbox());}catch(error){const r=safeApiError(error);return NextResponse.json({error:error instanceof Error?error.message:r.message},{status:r.status});}}
